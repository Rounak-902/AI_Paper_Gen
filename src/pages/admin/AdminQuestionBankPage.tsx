import QuestionBankPage from '../QuestionBankPage';

// Admin version of the question bank - same functionality
// Admin-specific actions can be added here later (bulk delete, etc.)
export default function AdminQuestionBankPage() {
  return (
    <div>
      <QuestionBankPage />
    </div>
  );
}
